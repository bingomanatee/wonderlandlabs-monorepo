import { TreeClass } from '../../lib';
import { TypeEnum } from '@wonderlandlabs/walrus';

describe('Forest', () => {
  describe('Leaf', () => {
    it('should have a value', () => {
      expect(1).toEqual(1);
    });
  });
});
