// ✅ Use Forestry hook for proper integration
const [counterValue, forest] = useForestryLocal(counterForestFactory);

// Usage in JSX
<Button onClick={() => forest.$.increment()}>
  +{counterValue.multiplier}
</Button>
<Button onClick={() => forest.$.doubleAndLog()}>
  Double
</Button>

// Hook handles subscription/cleanup automatically!
